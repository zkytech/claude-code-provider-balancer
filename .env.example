# Claude Code Provider Balancer - Environment Variables Example
# Copy this file to .env and modify the values as needed

# Logging configuration
LOG_LEVEL=DEBUG
LOG_FILE_PATH=logs/balancer.jsonl

# Provider configuration
PROVIDERS_CONFIG_PATH=providers.yaml

# Server configuration (can also be set in providers.yaml)
HOST=127.0.0.1
PORT=8080

# Application metadata
APP_NAME=Claude Code Provider Balancer
APP_VERSION=0.3.0

# Development settings
RELOAD=true

# Optional: Referrer URL for requests
REFERRER_URL=http://localhost:8082/claude_proxy

# Note: Provider API keys and URLs should be configured in providers.yaml
# This file is for general application settings only