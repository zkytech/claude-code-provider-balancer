# Docker Compose Environment Variables
# Copy this file to .env and adjust values as needed

# User and group IDs for proper file permissions in containers
# Use 'id -u' and 'id -g' to get your current user/group IDs
UID=1000
GID=1000

# Application port
PORT=9090